JWT_SECRET=supersecret_change_me
# Postgres connection URL (use your RDS endpoint in production)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ecom_db
# Set to 'true' if your DB requires SSL (common on RDS when enforcing TLS)
DB_SSL=false
PORT=3000
